#include <iostream>

using namespace std;

int main() {
	ios_base::sync_with_stdio (false);
	int x, y, z;
	int a, b;
	while (scanf("%d %d %d %d %d", &a, &b, &c, &d, &e) == 5 && a != 0 && b != 0 && c != 0 && d != 0 && e != 0) {
		
	}
	return 0;
}
